---
interface Props { label?: string; }
const { label = 'Advertisement' } = Astro.props;
---

<div class="card flex items-center justify-center min-h-[250px] w-full text-text-muted text-xs uppercase tracking-wide">
  <!-- Placeholder ad slot — replace with your ad network's script/tag (e.g. Google AdSense) -->
  {label}
</div>
